import * as React from "react";
import Button from "@mui/material/Button";

const ContactUsPage = () => {
  return (
    <div>
      <Button variant="contained">ContactUsPage world</Button>
    </div>
  );
};

export default ContactUsPage;
