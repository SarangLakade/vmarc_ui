import * as React from "react";
import Button from "@mui/material/Button";
import NavBar from "../component/NavBar";
const ProductPage = () => {
  return (
    <div>
      <NavBar />
      <Button variant="contained">ProductPage world</Button>
    </div>
  );
};

export default ProductPage;
